package flat/datastruct/list

class<E> {
  public native cloneData() -> E[] {
    let native d = new E[count]

    external {
      System.arraycopy(#{data}, 0, #{d}, 0, #{count});
    }

    return d
  }
}